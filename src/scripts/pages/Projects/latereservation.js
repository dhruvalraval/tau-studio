import Page from "../../classes/Page"

export default class LateReservation extends Page {
    constructor() {
        super({
            id: 'latereservation',
            element: '.work_display',
        })
    }
}

